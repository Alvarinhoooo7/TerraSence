<?xml version="1.0" encoding="utf-8"?>
<!--
    Uppercase section header for the sectioned tablet settings list.
    Mirrors the watch-android item_watch_setting_section_v2 style:
    poppins semibold 14sp, all caps, purple, with letter spacing for
    that quiet UPPERCASE breathing room.

    Used via <include> followed by the section's MaterialCardView of
    rows.
-->
<com.google.android.material.textview.MaterialTextView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/section_title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:fontFamily="@font/poppins_semibold"
    android:letterSpacing="0.05"
    android:paddingHorizontal="20dp"
    android:paddingTop="24dp"
    android:paddingBottom="8dp"
    android:textAllCaps="true"
    android:textColor="@color/tablet_settings_section_header"
    android:textSize="13sp" />
