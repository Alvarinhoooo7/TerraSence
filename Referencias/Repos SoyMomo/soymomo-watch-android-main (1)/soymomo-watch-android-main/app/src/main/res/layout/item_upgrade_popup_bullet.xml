<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_upgrade_popup_pill"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="12dp"
    android:paddingEnd="24dp"
    android:paddingTop="14dp"
    android:paddingBottom="14dp">

    <ImageView
        android:id="@+id/bullet_icon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="@drawable/bg_upgrade_popup_pill_icon"
        android:padding="8dp"
        app:tint="@color/momoFont"
        tools:srcCompat="@drawable/ic_check" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/bullet_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:fontFamily="@font/poppins_bold"
        android:textColor="@color/momoFont"
        android:textSize="16sp"
        android:textStyle="bold"
        tools:text="Planes desde los $4.990/ mes" />

</LinearLayout>
