<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/settings_card_design_white">

    <TextView
        android:id="@+id/variable_title"
        style="@style/ParentControlVariableFragmentTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/settings_fragment_initial_apps_title"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/allowed"
        android:textSize="16dp"
        />

    <com.google.android.material.chip.Chip
        android:id="@+id/allowed"
        style="@style/Widget.MaterialComponents.Chip.Choice"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:textAlignment="center"
        android:text="@string/settings_apps_allowed"
        app:chipBackgroundColor="@color/colorSecondary"
        android:textSize="14sp"
        android:textColor="@color/white"
        app:chipIconEnabled="true"
        app:layout_constraintEnd_toStartOf="@+id/blocked"
        app:layout_constraintTop_toTopOf="@+id/variable_title"
        app:layout_constraintBottom_toBottomOf="@+id/variable_title"
        android:layout_marginEnd="8dp"
        />

    <com.google.android.material.chip.Chip
        android:id="@+id/blocked"
        style="@style/Widget.MaterialComponents.Chip.Choice"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_marginEnd="16dp"
        android:text="@string/settings_apps_blocked"
        app:chipBackgroundColor="@color/grey_400"
        android:textSize="14sp"
        android:textColor="@color/grey_600"
        app:chipIconEnabled="true"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/variable_title"
        app:layout_constraintBottom_toBottomOf="@+id/variable_title"
        />

    <ViewFlipper
        android:id="@+id/view_flipper_allowed_apps"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@+id/variable_title"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:itemCount="10"
            tools:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_apps"
            tools:orientation="vertical"
            tools:scrollbars="vertical"
            android:paddingVertical="16dp"/>

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/progress_indicator"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:indicatorColor="@color/colorRed"
            app:trackColor="@color/white"
            android:indeterminate="true"
            android:layout_gravity="center">

        </com.google.android.material.progressindicator.CircularProgressIndicator>



    </ViewFlipper>


</androidx.constraintlayout.widget.ConstraintLayout>