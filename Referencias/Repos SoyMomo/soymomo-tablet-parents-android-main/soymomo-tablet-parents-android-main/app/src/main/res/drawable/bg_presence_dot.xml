<?xml version="1.0" encoding="utf-8"?>
<!-- Solid circle for the connection-status pill. Tinted at runtime per state
     via backgroundTintList (see TabletInitialMenuFragment.bindPresence). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/white" />
    <size
        android:width="7dp"
        android:height="7dp" />
</shape>
