<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container_dialog_pin_recovery"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/transparent"
    android:paddingHorizontal="8dp">

    <RelativeLayout
        android:id="@+id/linearLayout2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/dialog_main_shape"
        android:elevation="5dp"
        android:orientation="vertical"
        android:paddingHorizontal="8dp"
        android:paddingVertical="16dp"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout_editor_absoluteX="27dp">

        <ImageView
            android:id="@+id/icon_padlock"
            android:layout_width="71dp"
            android:layout_height="71dp"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_marginHorizontal="64dp"
            android:layout_marginTop="8dp"
            android:src="@drawable/padlock" />

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/icon_padlock"
            android:layout_alignParentStart="true"
            android:layout_alignParentEnd="true"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:gravity="center"
            android:text="@string/dialog_recovery_pin_title"
            android:textColor="@color/purple_recommended_app"
            android:textSize="24sp"
            android:textStyle="bold" />


        <TextView
            android:id="@+id/description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/title"
            android:layout_alignStart="@+id/title"
            android:layout_alignEnd="@+id/title"
            android:text="@string/dialog_recovery_pin_description"
            android:textAlignment="center"
            android:textColor="@color/purple_recommended_app"
            android:textSize="18sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/recover"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/button_height"
            android:layout_below="@+id/description"
            android:layout_alignStart="@+id/description"
            android:layout_alignEnd="@+id/description"
            android:layout_marginHorizontal="32dp"
            android:layout_marginTop="8dp"
            android:backgroundTint="@color/colorPrimary8"
            android:gravity="center"
            android:text="@string/dialog_recovery_pin_recover"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            app:cornerRadius="@dimen/button_corner_radius" />

        <ProgressBar
            android:id="@+id/progressBar"
            style="@style/Widget.AppCompat.ProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/recover"
            android:layout_alignStart="@+id/recover"
            android:layout_alignEnd="@+id/recover"
            android:layout_alignParentBottom="false"
            android:layout_marginBottom="8dp"
            android:progressBackgroundTint="@color/colorPrimary8"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/recover" />

    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>